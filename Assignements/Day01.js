var prompt = require('prompt-sync')();

// problem 1: conditional statement (if-else)
// Movie Theater
/*
const age = prompt("Enter your age: ")
if(age < 18) {
    console.log("You get a 20% discount");
}
else if(age == 18 && age == 65) {
    console.log("normal ticket price applies");
}
else {
    console.log("You get a 30% senior discount");
}
*/
// Output
// Enter your age: 70
// You get a 30% senior discount

// problem 2: Variables (var and const)
// Area of reactangle

/*
const length = prompt("Enter the length: ");
const width = prompt("Enter the width: ");

const area = length * width;

console.log("Area: ", area);
*/
// Output
// Enter the length: 4
// Enter the width: 4
// Area:  16

// Problem 3: Objects and properties
// Online store
/*
const product = {
    name: "Omkar Tarle",
    price: 5,
    inStock: true
}

console.log(product);
*/
// { name: 'Omkar Tarle', price: 5, inStock: true }

// Problem 4: Arrays
/* const  The `guestList` array is storing the names of guests invited to a party. The program checks if
a given name input by the user is included in the `guestList` array. If the name is found in
the array, it displays "Welcome to the party [name]". If the name is not found in the array,
it displays "Sorry, you're not on the guest list". */

/*
guestList = ["Omi", "Loki", "Tejya", "Lokya", "Shubhya"];

const input = prompt("Enter name plz: ");

if(guestList.includes(input)) {
    console.log(`Welcome to the party ${input}`);
}
else {
    console.log(`Sorry, you're not on the guest list ${input}`);
}
*/
 
    // youre organizing the party and want to track of the guest list create an array called guestlist. create an array called guestList and add the names of at least five guests then wetie a program that checks if a given name is onabort. guest list. if the name is found display name display "welcome to the party [name] otherwise display "FileSystemDirectoryReader, youere not on the guest list"

